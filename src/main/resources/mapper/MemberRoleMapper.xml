<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inthefridges.api.repository.MemberRoleRepository">
    <select id="findByMemberId" resultType="String">
        SELECT r.name
        FROM member_role mr
        JOIN role r ON mr.role_id = r.id
        WHERE mr.member_id = #{memberId}
        AND deleted_at IS NULL
    </select>

    <insert id="create" parameterType="MemberRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member_role
        (
            member_id,
            role_id
        )
        VALUES
        (
            #{memberId},
            #{roleId}
        )
    </insert>
</mapper>
