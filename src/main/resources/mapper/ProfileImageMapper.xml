<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inthefridges.api.repository.ProfileImageRepository">
    <select id="findByMemberId" resultType="ProfileImage">
        SELECT * FROM profile_image
        WHERE member_id = #{memberId}
        AND deleted_at IS NULL
    </select>

    <insert id="create" parameterType="ProfileImage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO profile_image
        (
            member_id,
            path
        )
        VALUES
        (
            #{memberId},
            #{path}
        )
    </insert>

    <delete id="delete" parameterType="Long">
        UPDATE profile_image
        SET deleted_at = NOW()
        WHERE
        member_id=#{memberId} AND
        deleted_at IS NULL
    </delete>
</mapper>
